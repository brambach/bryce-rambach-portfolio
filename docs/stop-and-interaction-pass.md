# Stops, privacy and interaction

The cafe is a coffee-and-projects break. Ordering adds a flat white to the rendered cup holder and replaces the order control with a receipt. The laptop and onward route remain available. Route and engine controls have less visual weight than the main actions.

Lake arrival explains the three choices: the view, projects, and the return race. View mode clears the arrival card and offers a way back to the choices. The existing driving and race mechanics are retained.

The entry control is visible on keyboard focus. Pointer and touch visitors use the car itself, with a small door cue after the opening film. The cue doesn't block the rendered car.

Project covers lift slightly, opening studies settle into place, editorial sections reveal as they enter view, and Arro frames change with a short page transition. Controls use a shallow pressed treatment. Reduced-motion preferences disable the decorative movement.

Public Arro assets are recaptured with fictional names and initials. The cover, study, enlarged frames and milestone use the same fictional cast. The prior images aren't in public or the production build. Original personal source files remain untouched outside this site. The lake introduction no longer states where Bryce's family lives.

## Validation

All 326 tests across 69 files passed with two workers. TypeScript and the production build passed. Browser checks passed for the six studies, direct routes, history, nested dialogs, focus, on-demand media, image inspection, responsive layouts and reduced motion. The Arro mobile frame was visually reviewed after transitions settled.

Physical-phone testing and deployment aren't included. Original app files weren't changed.

The stable local production preview completed the actual town trip: cafe arrival, coffee order, physical laptop, onward drive and lake arrival. Screenshots are in `output/stops/`. The view action now uses the same lake-position calculation as the arrival camera, rather than a fixed angle.
The corrected view was also verified using the returning-visitor route: start the lake replay, stay at the lake, open view mode, and return to the stop choices. `output/stops/lake-view-final.png` confirms the lake is framed and the return control is readable.
